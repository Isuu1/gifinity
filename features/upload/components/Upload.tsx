import React from "react";

//Styles
import styles from "./Upload.module.scss";

const Upload: React.FC = () => {
  return (
    <div className={styles.wrapper}>
      <div className={styles.container}>Upload</div>
    </div>
  );
};

export default Upload;
